/**
 * Interface for classes that can be pointed at by the PlanetPointer,
 * almost always associated with a planet if not directly parented to it.
 */
public interface PlanetAttachable {

	string DumpInfo();
}
